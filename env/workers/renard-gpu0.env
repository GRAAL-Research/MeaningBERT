# GTX 1080 Ti, 11 Go, la carte la plus rapide encore disponible. Elle finit bert/d_full,
# lance avant le redecoupage, puis prend l'autre run a 7320 pas, celui de roberta-base.
# Les deux runs longs partent ainsi sur les deux cartes rapides, pas sur la P5000.
GPU=0
SLICES="stsb-roberta-base|d_full"
HEADS=clamped
NUM_WORKERS=4
